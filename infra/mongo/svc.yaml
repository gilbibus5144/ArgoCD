apiVersion: v1
kind: Service
metadata:
  name: mongo-client
  namespace: mongo
spec:
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
      name: mongo
  type: ClusterIP
